#include "main.h"

/**
 * _abs - This will print the absolute value.
 *
 * @n: The parameter
 *
 * Return: This will return 0, 1, and -1.
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
