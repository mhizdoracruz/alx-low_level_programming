#include "main.h"

/**
 * _isdigit - the isdigit function
 *
 * @c: The values of c
 *
 * Return: Always return 0 or 1
 */

int _isdigit(int c)
{
	for (c = 48; c <= 57; c++)
	{
		return (1);
	}
	return (0);
}
